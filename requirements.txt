# Pinned for reproducibility. Local dev versions (Apple M2 Pro / MPS, Python 3.11.5).
# On Kaggle, torch/torchvision are preinstalled in the base image (CUDA build) — there,
# install only `timm` at the pinned version and leave torch/torchvision as the base image
# provides (see README for the exact Kaggle base image). The versions below are the
# local source of truth.
torch==2.1.2
torchvision==0.16.2
timm==1.0.27
scikit-learn==1.3.0
pandas==2.3.2
numpy==1.24.4
pyyaml==6.0.2
pillow==11.0.0
